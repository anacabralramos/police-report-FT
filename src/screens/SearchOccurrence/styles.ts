import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  // Estados Vazios
  emptyContainer: {
    alignItems: "center",
    justifyContent: "center",
    marginTop: 60,
  },
  emptyText: {
    color: "#8e8e93",
    fontSize: 16,
    marginTop: 16,
  },
});
