import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  emptyContainer: {
    alignItems: "center",
    justifyContent: "center",
    marginTop: 60,
    gap: 16,
  },
  container: {
    marginTop: 15,
    gap: 10,
    paddingBottom: 20,
  },
  flex: { flex: 1 },
});
